import React from 'react';

import Header from '../header';
import './styles.css';

const Move = () => {
    return (
        <div>
            <Header title="MOVE TO AZ" />
        </div>
    );
};

export default Move;